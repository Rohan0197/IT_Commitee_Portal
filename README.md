# IIM Bodh Gaya — IT Committee Portal

A responsive web portal for the IT Committee, IIM Bodh Gaya: browse and search events, register
for them, meet the team, and (for committee admins) manage the event calendar from a protected
admin panel — all backed by Firebase.

Built for **Task A (Core Build)** + **Track 1 (Web / Full-stack)**.

## Live links

- **Live site:** _add your deployed URL here_
- **GitHub repository:** _add your repo URL here_
- **Admin panel:** `admin/index.html` — test credentials: _add the admin email/password you create in Firebase Auth_

## Features completed

- [x] Events section rendered from Firestore (not hardcoded), loop-generated by `js/events.js`
- [x] Search by event name + filter by category
- [x] Team page with card layout (avatar, name, role, one-line bio), rendered from `js/team-data.js`
- [x] Event registration form with client-side validation, stored in Firestore (`registrations` collection)
- [x] Fully responsive layout (hand-written CSS media queries, mobile nav)
- [x] Authenticated admin panel (Firebase Auth) to create/edit/delete events, reflected live on the
      public events page via a real-time Firestore listener (`onSnapshot`)
- [x] Firestore security rules restricting event writes and registration reads to authenticated admins

## Tech stack

Plain **HTML, CSS and JavaScript** — no framework, no build step, no `npm install` required.
[Firebase](https://firebase.google.com/) (Firestore + Authentication) is loaded via the "compat" SDK
straight from Google's CDN with `<script>` tags, the same way you'd add any other JS library.

## Project structure

```
index.html               Home page
pages/
  events.html             Events list + search/filter + registration modal
  team.html               Team card grid
admin/
  index.html              Login gate + CRUD dashboard (one file, JS toggles which screen shows)
css/
  style.css                All styles, theme colors as CSS variables at the top
js/
  firebase-config.js       Firebase project config — fill this in, see below
  categories.js             Shared list of event categories
  validation.js             Shared form validation functions
  team-data.js               Static team member data
  events.js                  Events page logic (Firestore reads + registration form)
  admin.js                   Admin page logic (auth + CRUD)
  nav.js                      Mobile nav menu toggle
assets/                    Logos
firestore.rules            Firestore security rules
```

## Setup & run locally

### 1. Create a Firebase project

1. Go to the [Firebase console](https://console.firebase.google.com/) and create a new project.
2. Add a **Web app** to the project (Project settings → General → Your apps) and copy the config values.
3. Enable **Firestore Database** (start in production mode) and publish the rules in [`firestore.rules`](firestore.rules) (Firestore → Rules tab).
4. Enable **Authentication → Sign-in method → Email/Password**.
5. Under **Authentication → Users**, manually add one admin user (email + password) — this is the account used to sign in to the admin panel.

### 2. Configure Firebase in the code

Open [`js/firebase-config.js`](js/firebase-config.js) and paste in the six config values from step 1.2:

```js
const firebaseConfig = {
  apiKey: "...",
  authDomain: "...",
  projectId: "...",
  storageBucket: "...",
  messagingSenderId: "...",
  appId: "...",
};
```

### 3. Run it

There's no build step. Either:
- Open `index.html` directly in a browser, **or**
- Serve the folder with any static server if you'd rather use a local URL (e.g. the VS Code "Live Server" extension, or `npx serve`).

### 4. Add sample events

Sign in at `admin/index.html` with the admin user you created, then use **Add event** to create at
least six events across a few categories. They'll appear on the public Events page immediately.

## Deployment

This is a static site, so any static host works:

- **GitHub Pages:** push this folder to a repo, then Settings → Pages → deploy from the `main` branch.
- **Netlify / Vercel:** drag-and-drop the folder, or connect the Git repo — no build command needed.

## Screenshot

_Add a screenshot of the events page (desktop) and one mobile screenshot here after running the app locally._

## Admin panel (Track 1)

- URL: `admin/index.html`
- Test credentials: _fill in after creating the admin user in Firebase Auth_
- Demo: a create → edit → delete cycle is recorded in `_add link to your screen recording_`

## AI Usage

This project was built with **Claude Code** (Anthropic), used to:
- Scaffold the HTML pages, CSS, and JavaScript structure
- Write the Firebase integration (Firestore reads/writes, real-time listener, Auth sign-in/sign-out)
- Write the client-side validation logic, the Firestore security rules, and this README

All generated code was reviewed and is understood by the author before submission.
